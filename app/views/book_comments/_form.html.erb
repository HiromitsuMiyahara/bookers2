<%= form_with model: [book, book_comment], local: false do |f| %>
  <%= f.text_area :comment, cols: '80', rows: '5', placeholder: "コメントをここに", id: "comment_textarea" %>
  <%= f.submit "送信" %>
<% end %>