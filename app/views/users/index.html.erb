<h2>Users</h2>

<table>
  <thead>
    <tr>
      <th>image</th><th>name</th><th></th>
    </tr>
  </thead>
  <% @users.each do |user| %>
  <tbody>
    <tr>
      <td><%= users.title %></td>
      <td><%= users.name %></td>
      <td><%= book.title %></td>
    </tr>
  </tbody>
  <% end %>
</table>